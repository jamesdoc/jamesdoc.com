{
  "name": "jamesdoc",
  "version": "0.0.0",
  "description": "James Doc",
  "keywords": [],
  "homepage": "http://jamesdoc.com",
  "license": "MIT",
  "author": "James Doc",
  "scripts": {
    "build-task:scss-compile": "node-sass-chokidar --source-map true src/scss/ -o build/css",
    "build-task:autoprefixer": "postcss build/css/*.css --use autoprefixer -d build/css",
    "sass:build": "npm-run-all -p build-task:*",
    "sass:watch": "chokidar 'src/scss/**/*.scss' -c 'npm run sass:build'",
    "dev": "npm-run-all -p sass:*"
  },
  "dependencies": {
    "autoprefixer": "^7.1.1",
    "chokidar-cli": "^1.2.0",
    "node-sass-chokidar": "^0.0.1",
    "npm-run-all": "^4.0.2",
    "postcss-cli": "^4.0.0"
  }
}
